/// aquie se definiran cosas como como los tamaños del juego o el frame rate para qe cualquiera en el server pueda usarlos

const Frame_rate = 10;
const tamaño_grid = 20;

module.exports = {

    Frame_rate,
    tamaño_grid,
}